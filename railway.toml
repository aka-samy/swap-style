[build]
builder = "nixpacks"
buildCommand = "cd api && npm ci && npx prisma generate && npm run build"

[deploy]
startCommand = "cd api && npx prisma migrate deploy && node dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
rootDirectory = "/"
